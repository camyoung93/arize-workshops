# StreamFlix search-augment pipeline (generate_traces / create_evaluators /
# create_tasks / run_evals). The feature-store ingest scripts use the
# requirements files one directory up (arize-workshops/synthetic-data/).
# >=8.43: TemplateConfig/task enums are uppercase (SPAN/MAXIMIZE/TEMPLATE_EVALUATION);
# older 8.x used lowercase and will reject these scripts' values.
arize>=8.43.0
pandas>=2.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
python-dotenv>=1.0
